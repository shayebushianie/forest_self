# 异色植物根部独立融合实施计划

1. 将合成底座遮挡测试改为根颈可见与横向底座消失测试，先确认旧实现失败。
2. 在 `GachaDigSiteWidget` 内生成植物融合副本，删除前景土沿，结果态仅保留接触阴影并补绘短根须。
3. 更新六类植物和土地连续性回归，生成联系表与三档结果态截图并视觉复核。
4. 同步 `AGENTS.md` 与 `MEMORY.md`，完成 Release 构建、完整 CTest、部署、哈希与启动检查。
