# MEMORY.md

## 异色发掘场景化重构（2026-07-19）

- 已修复“重启或重建异色页面后植物消失”：根因是构造函数只刷新图鉴，没有用已解锁异色恢复场景。页面初始化时现会展示首个已解锁异色，并同步恢复结果文案；新抽取结果仍优先保留。UI smoke 新增页面重建回归与 `gacha-restored-unlocked.png`。
- 修复后的 Release 构建与完整 CTest 6/6 已通过，恢复截图已复核。根目录 `forest.exe` 已覆盖部署，构建文件与部署文件哈希一致，并通过三秒响应检查；SHA-256 为 `6C40F432F3F35B54A562876507B7C8CB1016BBBC408124D6F0EB2ED1E0205895`。
- 用户明确否定“前景土地覆盖根部”的融合方式。结果植物现改为在控件内部生成融合副本：底部宽泥团横向收尖并纵向渐隐，再以暖棕短根和浅接触阴影连接连续裸土；结果态不再绘制深色坑洞，土地层也不在植物之后覆盖根部。设计补充位于 `docs/superpowers/specs/2026-07-19-gacha-root-blend-design.md`。
- “异色发掘”使用低矮不规则林间空地和四列异色图鉴；厚重剖面平台已移除，土地由草缘、裸土、贴地浅阴影、3 处石块、4 处草簇、侧后方工具和克制裂隙构成。
- 结果植物按缓存的不透明边界缩放并锚定到统一地面点；全透明图片保持空状态。早期使用固定前景土沿遮挡素材底座的方案已废弃，现由植物融合副本独立处理根部，避免树下出现独立土块或根部被盖住。深色异色使用可读的中性亮色光晕。图鉴未解锁项显示植物剪影和一次“未发现”，已解锁项显示异色图、名称和稀有度。
- 文案统一为直接叙述：单次消耗、金币、重复返还、等待发掘、新异色、重复异色和失败原因；抽取价格、扣币、重复返还、持久化和成就触发逻辑未改变。
- UI smoke 新增合成根颈可见与横向底座消失、全透明图片、根部两侧裸土连续性、六类植物融合联系表、场景复杂度、12 张图鉴卡、四列三行、核心区域可见性及三档空态/结果态截图检查；发掘后验证图鉴即时更新。设计记录位于 `docs/superpowers/specs/2026-07-19-gacha-scene-design.md` 和 `docs/superpowers/specs/2026-07-19-gacha-root-blend-design.md`。
- 根部独立融合完成后，Release 构建与完整 CTest 6/6 通过；合成根颈、六类植物联系表和 1366×768、1440×900、1920×1080 结果态截图均已复核。根目录 `forest.exe` 已重新部署并完成三秒响应烟雾测试，SHA-256 为 `A1CAB735F8D17309A9A295582231FBB7A02E200F577661F4DCA28C0BA00DA082`。

## 标签总览弹窗圆角透明修复（2026-07-19）

- “总览筛选”弹窗改为透明无边框顶层窗口加内部圆角渐变卡片，保留浅绿色主体和深绿色圆角边框，同时彻底移除圆角外的方形纯色填充。
- UI smoke 新增顶层透明属性、四角 alpha 和圆角内部不透明度检查，避免只裁掉四角却同时丢失弹窗主体；实际 `forest-overview-filter.png` 已视觉复核。
- Release 构建与完整 CTest 6/6 通过；根目录 `forest.exe` 已重新部署、完成三秒响应烟雾测试并启动，SHA-256 为 `6B5433B1A3872110CEE4D05FCB8DE039B2840E56955CF79237BF63FF29EE8467`。

## 主页无框计时与引导外置（2026-07-19）

- 主页计时时长已移除半透明圆角卡片、边框和相关卡片几何，仅保留居中的大号时间文字；项目标签胶囊及其点击总览功能保持不变。
- “拖动光环调整时长”已从植物上方的圆环内部移到圆环外上方，成熟树和其他植物阶段均不再与提示文字重叠。
- UI smoke 将旧时长卡边界约束替换为无框计时边缘检查，并将成熟树提示测试改为圆环外可见性检查；目标测试及完整 UI smoke 已通过，1366×768、1440×900、1920×1080 三种主页截图已视觉复核。
- Release 构建与完整 CTest 6/6 通过；根目录 `forest.exe` 已重新部署、完成三秒响应烟雾测试并启动，SHA-256 为 `5701E8A60FDD6FD627790A5EF46C879A7C8A2A5158350FDD4F32D66FB6BD379C`。

## 成熟树提示遮挡修复（2026-07-19）

- 成熟树会遮挡“拖动光环调整时长”，根因是提示先于植物图片绘制。提示现改为在植物之后绘制，并使用低对比浅色胶囊确保所有植物阶段下可读。
- UI smoke 新增成熟树前景层渲染回归和 `mature-timer-guide.png`；修复前提示区域仅有 166 个可见差异像素，修复后测试通过。最新完整 CTest 6/6 通过，成熟树特写及 1366×768、1920×1080 主页截图已视觉复核。
- 根目录程序已重新部署并正常启动，SHA-256 为 `5A483F5278A600634A21A14549E9EF7B29B7C18B6E8C797A3741DA322CB1B2E1`。

## 主页面现代简约风格回退（2026-07-18）

- 主页保留现有计时模式、拖动调时、植物选择、项目标签总览入口和专注状态流程，仅对视觉做定向减法；未回滚历史代码或恢复 `version2` 布局。
- `PlantTimerWidget` 已移除横线、光点、叶片、内圈纹理、多层花环渐变和提示卡背景，改为单层环境光、细圆环、素净内圈和纯文字拖动提示；主页背景、模式切换、金币与提示语同步降低视觉重量。
- UI smoke 新增真实渲染复杂度和时长卡边界检查，并继续输出 1366×768、1440×900、1920×1080 三种主页截图。最新 Release 构建与完整 CTest 6/6 通过；三张主页截图已逐张复核，标签、时间和开始按钮完整可见，无装饰回归或文字重叠。
- 用户最初看到旧主页是因为修改仅位于隔离工作树，工作区根目录仍在运行 2026-07-13 的旧 `forest.exe`。2026-07-19 已将根目录程序替换为本轮构建，SHA-256 为 `635201B46DBF7AECFC6602406A9E227F1A306F47EE6DF20CFE3635971C60C1F3`；同时替换三项不匹配的 MinGW 运行库，消除启动时 `0xC0000005` 访问冲突。实际部署程序已显示无边框登录窗口并保持响应，登录后即可查看新版主页。

## 本轮网络连通性检查（2026-07-14）
- Clash Verge 与 mihomo 正在运行，系统代理已启用且本机代理端口在监听；但 WinHTTP 和当前终端的代理环境变量均为直连，导致 `gh auth login` 与 Git HTTPS 连接被重置。
- 仅为当前 Git/GitHub CLI 命令注入系统代理后，`https://github.com` 返回 HTTP 200；已完成 GitHub CLI 账号授权。未修改全局代理或 Git 配置。
- 远端 `main` 与本地历史无共同祖先，且包含便携包、构建目录和日志；为避免覆盖远端内容，本地基线与安装包流水线应以独立分支推送并通过草稿 PR 审阅，禁止强推远端 `main`。
- 已推送 `agent/local-release-baseline` 与 `release-installer-pipeline`；草稿 PR #1 从后者指向前者。三个安装包安全自测已通过；完整 Release、CTest 和真实 NSIS 安装器烟测仍待 CI 或具备 Qt/NSIS 的环境。
- 为更新仓库首页而不丢失旧内容，远端旧 `main` 已重命名为 `legacy-version-2`，当前本地 `main` 已发布为新的默认分支；草稿 PR #1 的目标已改为新 `main`。

## 实机安装包验证（2026-07-14）

- 在隔离工作树的 `build-release-installer` 完成 Release 构建；`ctest --test-dir build-release-installer -C Release --output-on-failure` 为 6/6 通过。
- 使用 NSIS 3.12 生成并实际安装、启动、卸载 `release/ForestFocus_Setup.exe`。安装包 SHA-256 为 `E05C6FDFD0551025B32DB7140A356074F193BAAC598DD09D42E69B52F5412CE4`；便携 ZIP SHA-256 为 `4992C19A26C23CC7CB0498CC0CC8032D2702A4AFEAC99AC294F428E355EECF51`。
- 安装烟测验证 Qt SQL 运行时、禁止的运行时数据/备份内容、三秒存活和静默卸载均通过。现有 `%LOCALAPPDATA%\Forest` 已存在，因此按保护规则跳过哨兵写入，未改动用户数据。
- 修复 `package_installer.ps1` 中字符串数组导致 `-NoBuild` 丢失的问题，并新增临时根目录下直接子目录的回归覆盖；同时允许其规范化路径恰好等于临时根目录，避免误拒绝安全的不存在安装目录。

## 安装包安全加固（2026-07-14）

- `package_installer.ps1` 与安装后烟测现在统一排除 SQLite WAL/SHM、`preferences.ini`、`restore_request.txt` 和备份/快照路径；阶段自测覆盖这些运行时数据，以及阶段目录中的 junction 外部目标不得被清理。
- 阶段自测不再借助子 PowerShell 进程判断失败：该包装器会吞掉终止异常并错误返回成功。现已改为直接捕获打包脚本异常；回归复现后，三个安装包安全自测均再次通过。
- 阶段目录删除会在删除前重新验证，并逐项以不穿越 reparse point 的方式删除；CI 在任何打包步骤前依次运行三个安装包安全自测。
- 若烟测创建了 `%LOCALAPPDATA%\Forest` 与 sentinel，会在卸载后的保留断言之后仅删除该测试创建的 sentinel 和空目录；既有 LocalAppData 数据绝不修改。
- 已执行并通过三个 PowerShell 安全自测：`installer_stage_selftest.ps1`、`installer_smoketest_selftest.ps1` 和 `installer_smoketest_ancestor_selftest.ps1`。本轮未执行 NSIS 或 Qt 构建/安装包验证，不能将这些自测等同于完整发布验证。

## 安装包 CI 与本地发布验证（2026-07-14）

- Windows CI 现会在便携包烟测后安装 NSIS、构建 `release/ForestFocus_Setup.exe` 并运行安装器烟测；无论结果如何都会上传安装器、`artifacts/*.sha256` 和 `artifacts/installer-smoke.log`，以及既有 UI 截图、CTest 日志和便携 ZIP。
- 规范发布入口已统一为源码根目录的 `installer/forest_installer.nsi`、`scripts/package_installer.ps1` 和 `scripts/installer_smoketest.ps1`；发布清单要求审阅两个 SHA-256 文件和安装器烟测日志。
- 本轮本地结果：`scripts/installer_stage_selftest.ps1` 退出码为 0。`build-architecture-upgrade` 在该 worktree 中不存在，因此 Release 构建和 CTest 均未启动（两条命令均退出码 1，CTest 没有测试结果）。`package_installer.ps1 -BuildDir build-architecture-upgrade -NoBuild` 退出码为 1，原因是本机没有 `windeployqt`；同时未找到 `makensis.exe`。因此没有生成便携 ZIP 或安装器，`artifacts/forest_portable.sha256` 与 `artifacts/ForestFocus_Setup.sha256` 均无本轮值。
- 安装器烟测命令退出码为 1，唯一原因是 `release/ForestFocus_Setup.exe` 不存在；它在创建或检查 LocalAppData sentinel 之前失败，因此未运行用户数据 sentinel 验证。尚未触发远程 CI，故没有可记录的工作流 URL、结果或下载产物审阅结论。

## 发布与质量基线（2026-07-14）

- 已批准“可复现安装包发布流水线”设计，详见 `project_docs/release-installer-design.md`。实现位于待合并的 `release-installer-pipeline` 分支，范围为规范化源码内安装脚本、以便携包为唯一安装输入、NSIS 安装/启动/卸载烟雾测试和 CI 工件；明确不包含自动发布、代码签名或产品功能改动。
- 详细实施计划见 `project_docs/release-installer-implementation-plan.md`。该分支已完成暂存隔离、NSIS 输入、安装烟雾、CI/文档四个验证单元，并通过三个 PowerShell 安全自测；因该隔离工作树没有 Qt/NSIS 构建依赖，完整 Release、CTest 与真实安装器烟测仍待 CI 或具备依赖的本机验证。

- 协作文件已统一到源码 Git 根目录：`AGENTS.md`（稳定规则）与本文件（动态记录）；根工作区旧 `AGENT.md`/`MEMORY.md` 已迁移，不再作为协作入口。
- `forest_ui_smoketest` 现覆盖 1366×768、1440×900、1920×1080 三种尺寸：首页断言标签、时长卡和开始按钮完整可见；森林页断言统计头部可见；六张截图输出到 `build-*/ui-fullscreen-snapshots/`。Windows 通过 `scripts/run_ui_smoke.ps1` 按实际 DPI 归一化 Qt 缩放，避免高 DPI 机器误报。
- CI 保留构建、6 项 CTest 和便携包启动检查；无论成功或失败都会上传 UI 截图、CTest 日志和便携包 ZIP。`package_portable.ps1` 不再包含机器专属 Qt 路径，仅使用参数、CMake 缓存或环境变量定位 Qt。
- 发布步骤见 `project_docs/release_checklist.md`。本轮仅完成便携包自动化；NSIS 安装包仍按清单手动在临时目录安装、启动和卸载验证，未扩大到 NSIS 环境自动化。
- 本轮验证：Release 构建成功；完整 CTest 6/6 通过；三种尺寸截图已人工复核；`package_portable.ps1 -SmokeTest` 成功；根目录部署版 `forest.exe` 已完成短暂启动检查。便携 ZIP SHA-256：`8CB15E7F4A21EE9F7AD7219AFE8747A8CF9899438CD3EF0C04E499D7F8C69703`。
- 已建立首个本地 Git 基线提交（仅源码、测试、CI、文档、资源和脚本；无远端推送）。

## 专注主页下半区样式恢复（2026-07-13）

- 按用户提供的旧版参考图，恢复首页计时环下方的视觉层级：更宽松的半透明时长卡片、与卡片衔接的圆角项目标签，以及较大的浅绿色胶囊形“开始专注”按钮；未改动计时、拖动调时、标签弹窗或开始专注业务逻辑。
- `FocusStartButton` 为首页开始按钮提供稳定的自绘圆角、悬停、按下、禁用和键盘焦点反馈，避免应用级样式表覆盖 `QPushButton` 圆角；按钮保留既有测试标识和点击信号。
- 验证：已人工审查最新 `focus-home.png`；Release 构建、全部 6 项 CTest、根目录 `forest.exe` 哈希核对和 3 秒隐藏启动烟雾测试均通过。部署 SHA-256：`9F7BFB83320045F04ADED2EA723E96AF7F061A530B402FF6F8944E3D62A03A16`。

## 森林空土地视觉清理（2026-07-13）

- 已从 `ForestDashboardWidget::drawIslandPanel()` 移除空土地中央的“种下专注”木牌，以及顶面前沿的一排绿色折角草芽；保留水洼、圆润边缘、分层侧面、地表纹理和侧面草花。
- 验证：已人工审查重新生成的 `navForest.png`，针对该页面的 UI 冒烟测试及全部 6 项 CTest 均通过。
- 部署状态：新 Release `forest.exe` 已构建完成，但检测到工作区根目录的 `forest.exe` 正在运行，未覆盖该文件，也未执行启动烟雾测试；关闭正在运行的程序后需重新部署并复核哈希和启动。

## 主页专注界面视觉升级（2026-07-13）

- 主页已完成两轮真实 Qt 截图迭代：背景升级为更有层次的森林渐变；模式切换、金币和提示语统一为圆润半透明卡片；开始专注按钮成为明确的绿色主操作。
- `PlantTimerWidget` 的计时器升级为带阴影、环境光和细节纹理的分层花环；内层加入拖动引导，项目标签与时长采用独立卡片，背景补充低对比叶片和萤光点，保留原有拖动、植物点击和标签点击交互。
- 已人工审查最新 `focus-home.png`，确认信息层级、卡通森林细节和主操作清晰。Release 构建、全部 6 项 CTest 和根目录程序启动烟雾测试通过。

## 森林土地浮岛视觉升级（2026-07-13）

- `ForestDashboardWidget::drawIslandPanel()` 的方形菱形土地已升级为圆润曲线顶面与三段式浮岛边缘：亮草边、绿色草层和深色底土，并保留原有植物排序和点击命中逻辑。
- 顶面新增多层水洼、细小地表纹理与石子；前沿保留草丛、小花和分层纹理。空土地中央木牌及顶面前沿的绿色折角草芽已按用户反馈移除。
- 已完成三轮真实 Qt 截图审查；最新 `navForest.png` 的土地边缘圆润、层次与细节符合用户提供的卡通浮岛参考。Release 构建、全部 6 项 CTest 和根目录程序启动烟雾测试通过。

## 总览记录数文字裁切修复（2026-07-13）

- “我的森林”页头的“总览”按钮下方记录数此前使用了越过页头边界的 14px 绘制矩形，导致字形在小区域截图中被裁切。
- 页头高度现为 74px；记录数使用总览按钮下方独立的 18px 居中绘制区，避免与按钮和下方内容重叠。
- 验证：已人工审查重新生成的 `navForest.png`，记录数完整可见；Release 构建、6 项 CTest 与根目录程序启动烟雾测试通过。

## 安装包重新打包（2026-07-13）

- 已使用工作区根目录 `installer/build_installer.ps1` 和 NSIS 3.12 重新生成 `release/ForestFocus_Setup.exe`，打包输入为本轮已验证的根目录 Release 程序及其运行时文件。
- 产物：35,116,874 字节；SHA-256 为 `9FB4DE84978B3ADF8B28774F62B45B67D2BF65B5424CFA9B910DDF5A61FFE289`。
- 本次未修改安装脚本；为避免影响当前用户可能已有的同名快捷方式和注册表项，未在真实用户环境执行静默安装/卸载验证。

## 主页标签直达总览（2026-07-13）

- 主页计时器下方的项目标签徽标现直接触发与“我的森林 → 总览”完全相同的无边框筛选弹窗，不再跳转至我的森林页面。
- 通过将 `PlantTimerWidget::sig_tagClicked` 直接连接到 `ForestDashboardWidget::overviewRequested` 复用现有弹窗实现，已删除旧的 `switchPage(1)` 跳转回调，没有新增重复弹窗代码。
- UI smoke 新增覆盖：触发主页标签信号后确认总览弹窗出现且主页面栈仍为索引 0。验证：Release 构建与全部 6 项 CTest 通过。

## 旧项目标签菜单代码清理（2026-07-13）

- 已删除未再实例化的 `TagPageController`，移除 `TagDialog` 源文件及其 CMake 编译项，并清理对应的 `TagManager` 前置声明和 include；总览弹窗的标签筛选、新增与持久化逻辑保持不变。
- `SocialPages` 现仅承载好友页面。UI smoke 中 `navTags` 的“不存在”断言保留，用于防止旧侧边栏菜单回归。
- 验证：重新生成 CMake 后，Release 构建和全部 6 项 CTest 均通过。

## 项目标签整合到总览弹窗（2026-07-13）

- 已移除“项目标签”侧边栏入口、独立页面和设置页中的“管理标签”按钮；时间守护页在主页面栈中的索引由 9 前移为 8。标签筛选和新增统一在“总览筛选”无边框弹窗中完成，主页标签徽标提供该弹窗的快捷入口。
- 弹窗内直接以四列圆角标签胶囊显示“无标签”和全部项目标签，彩色圆点取自 `TagDef::color`；白色为选中态，浅灰色为未选中态，不展示内部编码。末尾“＋”展开内嵌输入框，使用森林色板新增并立即持久化标签。
- 标签区最多同时显示 4×4 项（包含“无标签”和“＋”）；第 17 项起才显示纵向滚动条。已人工审查 `forest-overview-filter.png`，少量标签时无滚动条且视觉符合森林卡通弹窗风格。
- 验证：Release 构建通过；6 项 CTest 全部通过（含新增标签、选择筛选、导航索引的 UI smoke 覆盖）；工作区根目录 `forest.exe` 已更新，SHA-256 比对和 3 秒启动烟雾检查通过。

## 当前状态（2026-07-13）

- 源码位于 `C:\Users\24396\Desktop\Forest\forest_source_for_team\forest`，部署程序为工作区根目录 `forest.exe`，活跃 Release 构建目录为 `build-architecture-upgrade`。
- 上次记录的验证：Release 构建、六项 CTest、全屏截图生成及根目录程序启动烟雾测试均通过。该结论来自 2026-07-13 记录；后续源码或环境变化后须重新验证。
- 根目录不是 Git 仓库；源码目录已初始化 Git，但尚没有初始提交，全部当前文件显示为未跟踪。Windows CI 配置存在，但尚未在托管平台运行。

## 本轮文档审计（2026-07-13）

- 已通读根目录 `AGENT.md`、`MEMORY.md`，并以源码目录、CMake、格式配置和测试目标复核关键架构与命令。
- `AGENT.md` 已压缩为稳定规则；此前按日期的历史、过时页面索引和重复验证记录已移出，避免把临时进展当成长效约束。
- `MEMORY.md` 已收敛为当前决策、风险和交接信息；已移除敏感的演示账户口令及无须延续的逐次 UI 修改流水。
- 该文档审计阶段没有修改业务源码、构建配置、可执行文件或用户数据，也没有重新运行构建或测试。

## 主页计时文字裁切修复（2026-07-13）

- 用户截图显示主页 `25:00` 的数字底部被裁切。根因是 `PlantTimerWidget` 先将时间绘制区域延伸至组件底部，再向下平移；平移后的区域超出 QWidget 边界而被 Qt 裁剪。
- 时间绘制区域现在按字体实际高度计算，并将保留下移量限制在组件可见边界内，保留布局意图而不裁切数字。
- 验证：Release 构建通过；六项 CTest 全部通过；已审查重新生成的全屏主页截图，时间完整可见；根目录 `forest.exe` 已更新并完成启动烟雾测试。

## 我的森林总览标签筛选（2026-07-13）

- “我的森林”头部的“总览”已改为可点击按钮；点击后显示无边框模态弹窗，可选择“全部项目”或任一项目标签来筛选当前森林中的专注数据。
- 标签筛选只更新现有 `RecordFilter::tagId`，保留日期、植物和终态等已启用的高级筛选条件；“清除项目标签”同样不会重置这些其他条件。原“筛选”入口保持不变。
- UI 冒烟测试新增真实点击总览按钮、验证无边框标志、选择测试标签并确认 Dashboard 筛选状态更新的覆盖。Release 构建、六项 CTest、全屏森林页面审查和根目录程序启动烟雾测试均通过；根目录 `forest.exe` 已更新。
- 总览筛选弹窗已改为森林卡片风格：圆角绿植渐变、叶片徽章、标签选择卡和角色一致的按钮。下拉框仅显示“全部项目”或用户可读的项目标签名称；`tagId` 仅作为内部数据，UI 冒烟测试明确断言标签显示文本不含该内部编码，并保存弹窗截图供视觉复查。
- 总览筛选现不再使用下拉框：直接显示“全部项目”“无标签”及所有项目标签的单选卡，标签前的圆点采用 `TagDef::color`，内部 ID 仅用于筛选。标签较少时全部平铺；较多时才使用滚动容器，防止常见场景中卡片被裁切。
- 标签区已按用户参考图调整为横向圆角胶囊。当前标签使用白色高亮，其他标签为浅灰底；少于四项时不创建滚动容器，避免底部残影，更多标签才横向滚动。最新截图已人工检查。

## 全局弹窗卡通化（2026-07-13）

- `DialogPresenter::prepare()` 现统一为 `QDialog` 和 `QMessageBox` 启用无边框、样式化背景与模态行为；应用级对话框基础样式更新为圆角森林渐变卡片。
- 所有 10 个自定义对话框类、挑战页临时对话框、主窗口临时对话框、启动诊断提示与登录界面均已接入该规则。登录窗口使用专属的叶片标题和森林卡片输入框样式；UI 冒烟测试验证登录窗口与标准消息框均带无边框标志，并保存登录截图进行视觉检查。
- 验证：Release 构建、六项 CTest、登录和总览弹窗截图审查、根目录程序启动烟雾测试均通过；`forest.exe` 已更新。

## 已落地的关键决策

- Forest 维持模块化本地单用户桌面应用范围。`sessions.sqlite` 是专注历史事实来源；小型状态使用带版本和备份的 `StorageEnvelope`。安装模式和显式便携模式互斥。
- 专注生命周期固定为 `FocusSessionCoordinator`、`FocusController`、`FocusResultService`；结算账本与按记录 ID 的钱包/守护更新保证重启补偿和幂等。
- `PlantCatalog` 统一植物身份与资源；`PlantFactory` 负责具体行为创建；`FocusStatisticsQuery` 聚合我的森林统计并维护可重建内存索引。
- 页面已部分拆分：`CommercePages` 管理商城、成就与异色；`SocialPages` 管理好友和标签；`DashboardPageHost` 承载挑战与守护 Dashboard。`MainWindow` 仍较大，但只应在保留页面复杂度明显增长时再继续拆分。
- 用户反馈已统一到 `DialogPresenter`。UI 冒烟测试使用临时用户目录，避免改动真实账户状态。

## 当前产品与体验基线

- 首页会恢复最近有效的时长、植物、标签、计时模式、深度专注、自动延长和暂停设置；专注中隐藏侧边栏，托盘与快捷键支持显示、暂停/继续和放弃。
- 成功和放弃都使用单一结果面板；森林植物可打开对应记录详情；我的森林可按日期、标签、植物和终态过滤。
- 主页模式图标没有深色容器或选中底色。项目标签徽标刻意位于时间环下方 48px，计时器底部预留为 140px；调整此区域后必须做全屏视觉检查。
- 全屏基线覆盖主页、四个设置类别、森林、商城、成就、异色、好友、挑战、标签、守护，以及商城/异色/挑战/守护的操作后状态。最近记录的有效截图尺寸为 2561x1601。

## 已知风险、限制与待办

- 不添加云同步、远程身份、真实多人、角色、自动更新、额外货币或强制每日互动，除非获得新的明确产品里程碑。
- 远程 CI 和安装包的后续变更仍需在真实托管/临时安装目录验证；当前的“已通过”仅对应上次记录的本机验证。
- 旧对话框和历史文档可能仍因兼容或编译需要保留。删除前必须确认引用、迁移兼容和测试覆盖。
- PowerShell 可能把中文 UTF-8 文本显示成乱码；这是终端编码风险，不应触发机械重写。

## 容易踩坑

- 首次启动缺少可创建的数据文件应按空数据成功加载；损坏或未知版本数据则必须停止并提供可恢复路径。
- 修改专注记录字段、植物定义、统计周期或持久化时，必须同步相应自测并验证迁移/恢复；不要让 UI 根据记录列表位置猜测刚完成的记录。
- 统计按本地系统日期解释记录；项目分布按标签的专注次数，成功和放弃都保留在森林记录中，但奖励只来自有效终态流程。
- 异色抽取的费用和重复返还由 `GachaManager` 提供；重复返还不是新增收入，不能污染累计金币成就。
- 安装、打包、日志或文档中不得保留任何账户凭据或真实用户数据。
- `forest_ui_smoketest.exe` 不能脱离 Qt 环境包装直接启动；本机应通过 CTest 或 `scripts/run_ui_smoke.ps1` 运行，否则可能弹出平台插件初始化失败窗口。该测试程序不是交付给用户的应用。

## 后续交接

- 2026-07-26：森林与挑战 Dashboard 的自绘命中区域已改由透明 `QToolButton` 子控件承载。按钮会跟随绘制矩形、具备对象名/无障碍名称/提示/强焦点，并用原生按下后在同一按钮释放的语义触发；原有页面级鼠标坐标分发已移除。森林覆盖总览、周期、前后日期、筛选、设置和动态植物记录；挑战从全部现有 `HitRole` 自动生成控件。绘制层会读取按钮 hover、pressed、focus 状态绘制克制反馈。动态植物每次重绘均按当前绘制顺序置顶；`PaintedActionButton` 明确支持 Return/Enter，Space 保留 Qt 原生语义。`paintedDashboardsExposeKeyboardActions` 现覆盖语义元数据、非空几何、总览与挑战资源/任务键盘业务信号、外部释放取消触发和植物层级。2026-07-26 已构建并通过该定向 UI smoke；完整 UI smoke 仍应在合并所有并行改动后复跑。

- 下一次源码修改前，先确认 `build-architecture-upgrade` 是否仍可用；如需干净配置，使用独立构建目录并显式提供 Qt 路径。
- 对架构、持久化或 UI 的重大改动，完成后记录实际变更、运行的验证和仍存风险；长期规则变化才同步 `AGENT.md`。

## 安装包发布流水线验证（2026-07-14）

- 发布分支 `release-installer-pipeline` 已完成真实本机和 GitHub Actions 验证。最终 CI：<https://github.com/shayebushianie/forest_self/actions/runs/29345793967>，推送与 PR 触发均成功。
- CI 通过 Release 构建、6 项 CTest、三项安装安全回归、便携包启动检查、NSIS 安装包构建，以及静默安装、应用启动、禁止运行时数据检查和静默卸载。
- CI 工件已复核：`ForestFocus_Setup.exe` SHA-256 为 `40776CC9856925E15AE378248192BD120A4F0AA993671CEBF37E22AED82F2D07`；`forest_portable.zip` SHA-256 为 `01D2D04BB8F021E1797BDC3E0583B453915A74DFC2052DBE5B0F5B34B63A3C08`。安装烟雾日志以 `FINAL: PASS` 结束。
- 已人工复核 CI 生成的 1366x768、1440x900、1920x1080 首页和森林页截图：核心控件与森林统计头部均在可见区域。UI 烟雾测试改为按实际紧凑布局计算挑战签到的点击位置，并持续关闭仅用于测试的标准消息框，以消除 CI DPI/时序导致的假失败。
- 后续发布时，将 CI 工件中的安装包与便携包及各自 `.sha256` 上传到 GitHub Release；不要上传运行数据、日志、构建目录或 Qt SDK。

## 全页面交互、动效与无障碍改造（2026-07-26）

- 已移除 `AppStyle` 的全局透明度事件过滤器；普通控件改用状态颜色、边框和轻微按压位移。样式入口现接收文字比例与高对比参数，主页模式和暂停遮罩会管理初始焦点、Escape 关闭及焦点归还。
- 主页标签、植物、森林和挑战的自绘命中区现由可聚焦语义按钮承载，支持 Enter/Return、Space、tooltip、accessibleName、可见 hover/pressed/focus 状态和拖出取消；计时环使用 Slider 无障碍角色，公开 10–120 分钟当前值与步长，并支持方向键每次 1 分钟、PageUp/PageDown 每次 5 分钟。
- 侧栏使用单个可中断的 `200ms InOutCubic` 宽度动画；减少动效时立即切换。时间守护删除常驻 33ms 刷新，仅在可见且数据变化时运行一次 `200ms OutCubic` 进度过渡，隐藏时停止并完成到目标值，同时缓存缩放植物图。
- 成就卡改为浅色表面与深绿色正文，未解锁状态依靠锁图标和状态文案表达；正文不再通过低透明度弱化。UI smoke 新增交互语义、焦点遮罩、减少动效、动画中途反向、字号比例和高对比规则覆盖。
- 登录窗口使用透明顶层与自绘圆角渐变表面，圆角外不再出现方形纯色填充；UI smoke 断言四角透明且中央表面不透明。
- 验证：`build-ui-simple` Release 全量构建成功，6 项 CTest 全部通过；UI smoke 生成 80 张截图，覆盖 1366×768、1440×900、1920×1080 的登录、主页、森林、商城、成就、异色、用户、挑战、守护和四个设置分类，并额外检查主页 90%、100%、125% 文字比例及 125% 全页面状态。已人工抽查 125% 主页、成就、挑战、异色和设置截图，未见核心控件裁剪；构建输出 `forest.exe` 完成三秒响应检查。未创建提交、未推送、未重新打包。

## Issue 跟踪方式（2026-07-26）

- 用户确认后续缺陷、改进项和发布待办使用 GitHub Issues 跟踪。本次只确认协作方式，尚未创建或修改具体 Issue；创建前仍需明确待办范围和验收条件。

## “森林玻璃”设置界面（2026-07-26）

- 设置界面已从 `MainWindow` 拆分为独立 `SettingsPage`；页面只提供类型化状态、更新方法和用户动作信号，偏好保存、黑名单、守护目标及备份/恢复/导出等服务仍由 `MainWindow` 协调。四个分类及顺序保持不变。
- 页面改为深绿色半透明分类栏、森林渐变背景和近实色圆角分组列表。植物、模式和标签支持整行打开选择列表，植物与标签只显示用户可读名称；正计时模式仍会禁用专注时长。
- 新增 `SproutToggle`：保留原生复选框无障碍语义，以圆点/萌芽图形和“未启用/已启用”文字表达状态，支持点击、Space、Enter、Return、拖出取消、焦点环及 `150ms OutCubic` 可中断反馈；减少动效时立即停止并切换。
- UI smoke 新增萌芽状态钮交互、分类键盘切换、无障碍名称、整行选择弹出位置、主页与设置同步、即时偏好保存和设置页结构渲染覆盖。截图覆盖四分类的 1366×768、1440×900、1920×1080，并包含 125% 文字和高对比度状态；人工复核未见横向溢出或方形底色泄露，125% 下较长内容通过纵向滚动保持可达。
- 验证：`build-ui-simple` Release 全量构建成功，6 项 CTest 全部通过。构建仍报告两条既有 `MyForestDialog.cpp` 有符号/无符号比较警告，本轮未作无关修改。未部署、未打包、未提交或推送。
- 后续用户反馈实际打开的界面没有变化。排查确认工作区根目录 `C:\Users\24396\Desktop\Forest\forest.exe` 仍是 2026-07-19 的旧程序，而新程序只在隔离工作树的构建目录中；这是上一轮遵守“不部署”约束造成的版本入口差异，并非设置页代码未生效。现已将通过测试的构建输出部署到根目录，源/目标 SHA-256 均为 `1FDB0E6B6048628B3C00E0AC9DCFDB39BB5C9C6E9755E49B9038581714E90FB0`，并确认该路径启动后进程响应正常。

## 当前改动动效复审（2026-07-26）

- 使用 `review-animations` 对当前差异中的动画与交互进行了两轮审查。首轮结论为 Block：侧栏 `maximumWidth` 动画、主页模式 `maximumHeight` 动画、萌芽钮键盘切换动画，以及选择行拖出后 pressed 反馈滞后不符合高频响应、性能或直接操控标准。
- 已删除两处布局属性动画，侧栏和主页模式内容改为即时切换；萌芽钮只在有效鼠标点击时运行 `150ms OutCubic` 可中断过渡，Space、Enter、Return 和程序同步即时更新；整行选择的 pressed 状态会随指针拖入/拖出实时变化。
- 守护进度保留 `200ms OutCubic` 自绘状态过渡：它只在可见且数据变化时运行，从当前值重定向，隐藏或减少动效时停止。第二轮审查确认没有 feel-breaking、高频键盘动画或易替代的布局动画，结论为 Approve。
- 验证：新增定向交互断言先复现三类阻断，再使其通过；Release 全量构建成功，6 项 CTest 全部通过，`git diff --check` 无错误，源码扫描未发现宽度/高度属性动画。最新程序已部署到工作区根目录并启动，SHA-256 为 `6E6A243673C14682D9ED75E034D1C33C99B58634F415F9137B8FC4B8B7A4CC65`，进程响应正常。未提交、推送或打包。

## 主页植物选择 Popup（2026-07-26）

- 主页植物选择由 `dialog.exec()` 应用级模态窗口改为 `Qt::Popup | Qt::FramelessWindowHint` 非模态选择层，并由 `QPointer<QDialog>` 保证同一时间只有一个实例。
- 点击外部空白或按 Escape 只关闭 Popup，不修改或保存植物；只有点击已解锁植物卡片才同步 `SettingsPage` 和主页计时组件。植物卡片增加内部 `plantType` 属性用于稳定测试，不向用户显示编码。
- UI smoke 在测试沙箱中覆盖外部点击取消、Escape 取消、取消后植物值不变，以及选择另一种已解锁植物后即时同步。测试曾因向 `QWidget` 直接注入点击而绕过 Qt Popup 的窗口系统路由；改为向主窗口 `QWindow` 发送点击后，完整套件稳定复现真实交互。
- 验证：Release 全量构建成功，6 项 CTest 全部通过，`git diff --check` 无错误。设计与实施清单位于 `docs/superpowers/specs/2026-07-26-home-plant-popup-design.md` 和 `docs/superpowers/plans/2026-07-26-home-plant-popup.md`。最新程序已部署到工作区根目录并启动，SHA-256 为 `6C6FA68EC774419C6B17FCD6B9179423E21DFF99E4C21EDAED13DFB34C3A515E`，进程响应正常。未提交、推送或打包。

## “我的森林”植物交互性能与圈定框（2026-07-26）

- 卡顿根因是植物语义按钮的 hover、pressed、focus 事件触发整页重绘，而绘制路径会对约 1300×1280 的原始植物图片重复去背景、灰度转换、缩放和碰撞布局；原固定命中框也比可见植物宽约 1.8–2.4 倍、高约 2.4–2.8 倍。
- 森林页面现缓存静态绘制层及每种植物的裁切缩略图；数据、筛选、周期、日期、尺寸或 DPR 变化时才重建。圈定反馈取自实际不透明绘制边界并外扩 4px，点击区额外使用不可见缓冲且最小为 44×44px，记录 ID、层级、键盘触发和拖出取消语义保持不变。
- TDD 红灯基线为 10 次悬停循环约 7504ms；修复后独立诊断为 4ms，样例按钮由 75×103px 收敛到 53×52px。新增测试覆盖绘制/反馈/命中矩形、逐植物记录 ID、Enter/Space、拖出取消、静态层代次和 500ms 性能上限。
- Release 全量构建和 6 项 CTest 通过；已人工检查 1366×768、1440×900、1920×1080 的普通、悬停和焦点截图，植物与圈定框无明显错位。根目录 `forest.exe` 已更新，SHA-256 为 `0AE4045322DC55B0A918F270187B9DD7D54CD763030E80B7ED394C0107417328`，三秒启动检查中进程正常响应。未提交、推送或打包。

## 主页透明语义按钮遮挡修复（2026-07-26）

- 主页中央的大块纯色矩形和标签处的小矩形并非植物资源或绘制坐标故障，而是实际控件为 `QToolButton`，局部样式却使用 `QPushButton` 选择器，导致透明语义覆盖按钮退回原生不透明外观并遮住父控件绘制内容。
- 新增回归测试，比较语义子按钮参与和不参与时的主页局部渲染；修复前检测到 75,559 个泄露像素。局部样式选择器改为 `QToolButton` 后空闲态保持透明，原有 hover、pressed、focus、鼠标取消、点击和键盘语义不变。
- Release 全量构建和 6 项 CTest 通过；已人工检查 1366×768、1440×900、1920×1080 主页截图，植物、标签、计时和开始按钮均完整显示。根目录 `forest.exe` 已更新，源码与部署文件 SHA-256 均为 `A040B208CB00A049F05E7DD9D1E2803962779AEFA8DFDEC1DA7AAFA8ECE77217`，三秒启动检查中进程正常响应。未提交、推送或打包。

## 连续森林画布设置页（2026-07-26）

- 设置页已去除深色整块分类栏、内容面板与分组白卡的重复表面，改为单一浅森林内容表面；四个分类使用窄幅透明导航、低对比选中底和短绿色强调线，内容通过标题、留白与细分隔线组织。
- 组合框、数值框和输入框默认改为浅色填充与透明边界，仅在 hover/focus 时增强；萌芽状态钮保留唯一状态边界。植物、模式、标签三类选择行保持整行打开列表，普通状态行点击标题区也会聚焦或切换尾部控件，拖出取消语义不变。
- 新增结构回归：旧分类栏单独绘制 150,780 个不透明像素时测试失败；修复后验证单一表面、所有设置分组透明、三类选择行整行点击、状态行整行切换以及高对比圆角无方形底色泄露。设计与实施记录位于 `docs/superpowers/specs/2026-07-26-continuous-settings-surface-design.md` 和 `docs/superpowers/plans/2026-07-26-continuous-settings-surface.md`。
- Release 全量构建和 6 项 CTest 通过；已人工检查四分类在 1366×768、1440×900、1920×1080、125% 字号及高对比状态下的截图，未见双重外壳、白卡回归、裁剪或方形圆角泄露。根目录 `forest.exe` 已更新，源码与部署文件 SHA-256 均为 `7635FBF453027E99B1A9ADD212DBA80025128B46E78A3F5C1A2664C5E10CF733`，三秒启动检查中进程正常响应。未提交、推送或打包。

## 全应用焦点框清理（2026-07-26）

- `AppStyle` 现统一跟踪指针与键盘输入方式，并通过 `keyboardFocusVisible` 动态属性控制提示。鼠标、触摸或数位笔按下会立即清除持续键盘提示；Tab、方向键、PageUp/PageDown、Home/End、Enter 和 Space 会进入键盘模式。安装入口可重复调用，正式应用与 UI 测试使用同一逻辑。
- 全局按钮、输入控件、组合框、数值框、列表、单选框和复选框已移除无条件焦点边框与原生虚线框；鼠标聚焦的输入控件仍可编辑，只用背景变化表达编辑状态。高对比模式继续增强颜色，但不恢复包围式黄色框。
- 设置分类保留真实选中底和绿色左线，键盘时只把左线改为金色；设置选择行使用短左线，萌芽状态钮使用内部金点，主页开始按钮使用底部短线，主页标签使用短下划线，主页植物使用植株顶部小圆点，森林和挑战使用局部轻填充与角点，不再绘制完整圆角矩形。
- 新增回归验证输入方式切换、鼠标聚焦不改变普通按钮/主页植物/设置选择行的静态画面、鼠标操作立即清除键盘提示及输入框继续可输入。三种分辨率均生成主页植物、设置分类和森林植物的指针/键盘截图；人工复核未见浅色外框、黄色包围框或虚线框，森林指针截图与普通截图像素一致。
- Release 全量构建和完整 CTest 6/6 通过；仅保留两条既有 `MyForestDialog.cpp` 有符号/无符号比较警告。根目录 `forest.exe` 已更新，源码与部署文件 SHA-256 均为 `BCEA20BF56644D3407E0C797B130F9BD7AD24ACCD9BBBA1E04A6CC47D5940644`，三秒启动检查中进程存活且响应正常。未提交、推送或打包。
