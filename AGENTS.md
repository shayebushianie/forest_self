# AGENTS.md

## 项目概览

- Forest Focus 是单用户 Windows 桌面专注应用，使用 C++17、Qt 6 Widgets、QSS 和 CMake。
- 产品包含专注计时、植物成长、我的森林（含统计）、植物商城、成就、异色发掘、好友、挑战、项目标签、时间守护和系统设置。
- 统计只属于“我的森林”，不得恢复独立的“数据统计”侧边栏页面。

## 路径与目录

- 本文件与 `MEMORY.md` 位于源码与 Git 根目录；工作区为 `C:\Users\24396\Desktop\Forest`，部署程序为工作区根目录 `forest.exe`。
- `src/core`：业务服务和专注流程；`src/storage`：SQLite、备份、恢复、锁与用户存储；`src/config`：目录和偏好；`src/plant`：植物行为；`src/ui`：窗口、组件与页面控制器；`src/tests`：CTest 自测。
- `project_docs/adr` 记录数据存储、数据目录和产品范围等架构决策；根目录 `开发思路与架构设计.md` 是架构说明。
- 根目录 `README.md` 面向最终用户；`README_FOR_TEAM.md` 面向源码协作。不要把实现细节或构建步骤混入用户 README。

## 构建、测试与发布

- 既有 Release 树：`cmake --build build-architecture-upgrade --config Release --parallel 4`；干净构建：`cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="<Qt-root>"`。
- 完整测试：`ctest --test-dir <build-dir> --output-on-failure`。当前 CMake 注册六项测试：storage、focus resilience、user security、plant catalog、statistics、UI smoke。
- 不得把机器绝对路径写入 CMake 或打包脚本；Qt 仅可通过 `-QtDir`、CMake 缓存或环境变量定位。
- 成功构建后才可部署：复制构建输出的 `forest.exe` 到工作区根目录，并进行短暂启动烟雾测试。修改安装脚本后，还要在临时自定义目录完成安装、启动与卸载验证。
- Windows 安装包的规范发布入口是源码根目录的 `installer/forest_installer.nsi`、`scripts/package_installer.ps1` 和 `scripts/installer_smoketest.ps1`，输出 `release/ForestFocus_Setup.exe`；安装包绝不打包或删除用户数据。

## 代码与文档风格

- 遵循 `.clang-format`：Chromium 基础风格、4 空格缩进、110 列、附着大括号、不自动排序 include。匹配周边代码，不进行无关重构或格式化。
- 文本源码与文档使用 UTF-8 BOM 和 CRLF；终端中文显示异常时先检查编码，禁止仅因显示乱码而重写文件。
- 保持命名和模块边界清晰。新增业务代码优先使用窄服务、类型化领域值和现有注册表，避免通用“大管家”、魔法数和重复映射。
- 提交应小而可验证，使用简短祈使式主题并说明变更范围；未获得用户明确要求时，不创建提交、推送或改写历史。
- 缺陷、改进项和发布待办统一使用仓库的 GitHub Issues 跟踪；Issue 应包含可验证的完成条件，并引用相关测试、截图或日志。仅在用户明确指定范围或确认待办清单后创建、关闭或修改 Issue。
- 仅在规则、构建方式、架构边界或长期协作方式改变时更新本文件；重要进展、验证、风险和待办更新 `MEMORY.md`。

## 架构边界

- `sessions.sqlite` 是专注历史的唯一事实来源；小型持久状态使用 `StorageEnvelope`。`PlantCatalog` 是植物身份和资源元数据的唯一注册表；`FocusStatisticsQuery` 是统计聚合的唯一边界。
- 专注终态必须经 `FocusSessionCoordinator` -> `FocusController` -> `FocusResultService`。UI 只能展示结果和刷新页面，不能直接写专注记录、钱包、成就、挑战或守护进度。
- `FocusSettlementManager`、`CoinManager` 和 `GuardianManager` 的按记录 ID 幂等结算不可绕过。终态写入失败时必须保持待结算并由启动补偿处理。
- `MainWindow` 只承担应用壳、控件组合和跨页协调；商城/成就/异色使用 `CommercePages`，好友使用 `SocialPages`，已有 Dashboard 使用 `DashboardPageHost`。项目标签入口仅为总览弹窗，禁止恢复独立标签页面。不要把页面业务重新堆回 `MainWindow`。
- 新的用户隔离功能优先接入 `UserFeatureServices` 的完整 `load()`/`save()` 生命周期。页面代码不得直接读写持久文件或 `QSettings`；使用现有服务和 `UserPreferences`。
- 所有用户可见弹窗（含登录、启动诊断、确认、结果与遗留功能窗口）必须无边框并保持森林卡通卡片风格。页面反馈使用 `DialogPresenter`；自定义对话框必须调用 `DialogPresenter::prepare()` 并使用明确的主、次或危险按钮角色，禁止新增原生带标题栏的 `QDialog` 或 `QMessageBox`。

## 数据安全与兼容

- 用户数据优先于可执行文件。安装模式使用 `%LOCALAPPDATA%/Forest`；仅同级 `forest.portable` 标记可启用便携 `app_data`。绝不删除、覆盖或迁移用户数据、备份和 `.bak` 文件，除非有迁移、备份、验证和用户可恢复路径。
- 二进制状态必须通过 `StorageEnvelope` 的版本、校验、原子写入和备份机制保存。旧格式仅可作为经验证的迁移输入；遇到未知未来版本或不可恢复损坏必须停止加载，不能静默重置为空数据。
- 持久化枚举值保持兼容；任何 schema 变化使用迁移并保留备份。数据库和用户状态载入失败时，不显示主窗口。
- 运行前先取得 `ApplicationInstanceLock`。活跃专注记录每 30 秒检查点；下次启动时将未完成记录恢复为放弃且无奖励。
- 不记录、提交、打包或打印密码、密钥、Token、真实用户数据、日志、运行时数据、Qt SDK、构建产物和备份。安装包不得包含根目录 `app_data`、`users.dat` 或日志，卸载也不得删除用户数据。

## 产品与 UI 约束

- 保持本地单用户范围：未经明确里程碑批准，不添加云同步、远程身份、真实多人、角色权限、微服务、自动更新、额外货币或强制每日互动。
- 空闲主页优先呈现时长、项目标签、植物和一个开始动作；专注中隐藏导航与游戏入口。成功与放弃各使用一个整合结果面板，放弃仍保留记录且使用非惩罚性文案。
- 空闲主页使用简化植物圆环、单层柔和环境光和可点击的项目标签卡；计时时长仅显示无框文字。避免叶片纹样、萤光点、多层花环和厚重阴影。拖动时长的引导必须位于计时圆环外，“开始专注”主操作必须清晰可见，并在所有基准分辨率中避免裁切。
- 主页植物入口使用非模态无边框 Popup；点击外部空白或按 Escape 必须关闭且保留当前植物，只有明确点击植物卡片才允许写入选择。同一时间不得叠加多个植物选择层。
- 森林中的植物必须可追溯到持久化专注记录；植物、标签、统计、成就、挑战和奖励使用同一记录及共享注册表。
- “异色发掘”使用低矮不规则林间空地、裸土、石块、草簇和异色光迹组成的克制卡通场景，收藏使用四列图鉴卡片；不得恢复厚重剖面平台、通用矩形面板或简单几何图形堆叠，也不要添加无语义的繁杂装饰。结果植物必须以统一地面锚点和接触阴影融入土地；植物图层应独立淡出素材自带的宽泥土底座并保留根颈，禁止用后绘制的土地或坑沿遮挡根部。页面重建时若已有解锁异色，应恢复展示一个已解锁植物，不得无条件退回空坑状态。状态文案直接叙述操作与结果。
- “我的森林”的项目标签筛选必须直接展示横向圆角标签胶囊，并在标签名前显示对应颜色圆点；当前选中标签使用白色高亮，未选中标签使用浅灰底。不得在该入口改回下拉选择或向用户显示内部 `tagId`。
- 植物资源统一经 `PlantImageUtils::loadPlantIcon()` 处理。新植物通过 `PlantCatalog`、具体实现和 `PlantFactory` 同步添加，不在 UI 或控制器中新增植物类型 switch。
- 图标按钮必须可由键盘聚焦，并具备可见禁用态和 tooltip。UI 改动必须检查 1366x768、1440x900、1920x1080 截图，关注重叠、裁切、hover/focus 与不可达操作；更新相应 `forest_ui_smoketest` 覆盖。
- 自绘交互区域必须由可聚焦的语义控件承载，支持 Enter/Return、Space、稳定的 accessibleName、tooltip 和可辨识的键盘焦点提示；不得仅依赖父控件坐标命中。透明语义覆盖控件的局部 QSS 必须匹配实际控件类型，空闲态不得改变父控件绘制结果。键盘和高频操作必须即时响应，禁止为反馈动画宽度、高度等布局属性；其余非即时运动必须可中断并服从 `reducedMotion`，禁止用常驻高频定时器驱动纯装饰动画。
- 指针操作不得留下焦点外框或原生虚线框；键盘焦点只能使用短线、圆点、角标或轻量填充等非包围式提示。不得通过 `Qt::NoFocus` 删除键盘能力；统一使用 `AppStyle` 的输入方式跟踪区分指针与键盘焦点，高对比模式也不得恢复完整矩形焦点框。
- 自绘密集页面的 hover、pressed 和 focus 反馈不得重复图片去背景、灰度转换、缩放或布局碰撞计算；反馈范围必须来自实际不透明绘制边界，易点击所需的扩大命中区应保持不可见。
- 设置页保持“连续森林画布”结构：轻量分类导航、单一浅森林内容表面、以留白和细分隔线组织的连续设置行及即时保存，不增加深色整块分类栏、多张白色卡片或“应用”按钮。选择项和普通状态项应支持整行点击，输入控件默认使用弱边界并仅在 hover/focus 时增强。布尔设置使用保留复选框语义的萌芽状态钮，以文字同时表达状态，禁止复制滑轨式 Apple 开关；选择项只显示用户可读名称，不暴露内部编码。

## 项目标签入口规则

- 不提供独立的“项目标签”侧边栏或页面；标签筛选和新增统一在“我的森林”的“总览筛选”无边框弹窗中完成。
- 主页计时器下方的当前标签徽标点击后必须直接打开同一个“总览筛选”弹窗，且不得切换离开主页。
- 标签以四列圆角胶囊网格显示，名称前使用 `TagDef::color` 对应的彩色圆点；选中为白色高亮，未选中为浅灰色，禁止显示内部 `tagId` 或改回下拉选择。
- 新增标签的“＋”必须始终位于标签末尾。标签栏可随内容增长至 4×4；包含“无标签”和“＋”在内超过 16 项时才显示纵向滚动条。

## 开发流程与检查清单

修改前：

1. 阅读本文件、`MEMORY.md`、相关 ADR、目标模块和现有测试；先确认需求、数据所有权与兼容影响。
2. 检查工作树，保留用户已有的无关改动。不要删除历史文件、生成物或用户数据来“清理”环境。
3. 对多步骤工作明确最小计划及可验证成功标准；需求含义或权限范围不明时先说明并询问。

修改后：

1. 删除本次产生的无用引用、变量或文件，保留与需求无关的既有问题并在回复中说明。
2. 运行与变更相称的目标测试；存储、统计、植物或专注流程变更还须跑完整 CTest，UI 变更须生成并审查全屏截图。
3. 构建或部署失败时如实报告，不伪称验证通过。完成重要修改后，简洁更新 `MEMORY.md`：变更、验证、风险或后续触发条件。

## 禁止事项

- 不进行未经请求的重构、功能扩展、数据删除、依赖升级或视觉重做。
- 不直接写持久文件、复制业务状态到 UI、重建重复植物/统计注册表、绕过专注结算链或单实例锁。
- 不提交生成目录、运行数据、凭据或本机路径；本地 Git 尚无初始提交时，未经用户明确要求不得擅自提交或推送。
